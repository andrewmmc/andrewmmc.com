---
import type { ImageMetadata } from 'astro';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import type { BlogLanguageLink } from '../utils/blog';

interface Props {
  title?: string;
  description?: string;
  keywords?: string;
  ogType?: string;
  articleDate?: string;
  updatedDate?: string;
  heroImage?: ImageMetadata;
  lang?: string;
  languageLinks?: BlogLanguageLink[];
  noindex?: boolean;
}

const {
  title,
  description,
  keywords,
  ogType,
  articleDate,
  updatedDate,
  heroImage,
  lang = 'en',
  languageLinks,
  noindex,
} = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <BaseHead
      title={title}
      description={description}
      keywords={keywords}
      ogType={ogType}
      articleDate={articleDate}
      updatedDate={updatedDate}
      heroImage={heroImage}
      lang={lang}
      languageLinks={languageLinks}
      noindex={noindex}
    />
  </head>
  <body>
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-white focus:text-amber-700 focus:rounded focus:shadow-md focus:text-sm"
    >
      Skip to main content
    </a>
    <Header />
    <main id="main-content" class="max-w-container mx-auto px-6 w-full">
      <slot />
    </main>
    <Footer />
  </body>
</html>
